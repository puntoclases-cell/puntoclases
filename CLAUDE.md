# CLAUDE.md — PuntoClases

Web de clases particulares. Continuás un proyecto **EN PRODUCCIÓN**.
Arrancá del estado de abajo; **no re-diagnostiques lo ✅**.

## Autonomía (regla fija)
- Claude Code corre TODO de forma autónoma sin pedir OK: lecturas, ediciones de código, comandos CLI, secrets, deploys, y migraciones a la base (incluida producción).
- ÚNICA obligación antes de una migración que MODIFIQUE o BORRE datos existentes (no para agregar columnas/tablas nuevas vacías): hacer un backup/dump de las tablas afectadas primero, automáticamente, sin preguntar. Dejar registrado en el CLAUDE.md qué se respaldó y dónde.
- Si una operación falla, revertir lo que se pueda y reportarlo a David. Nunca dejar la base rota a medias.

## Cómo trabajás conmigo (reglas fijas)
- Rioplatense, **output mínimo**. Tengo teclado complicado → **minimizá MI tecleo**: dame bloques para copiar y opciones para tildar.
- Antes de pedirme algo, **resolvelo vos con tus herramientas**: leé/editá/corré el código, `curl` para probar, Supabase CLI para datos. Pedime solo (a) un OK o (b) lo que tus herramientas no alcanzan (navegador/Dashboard).
- **CERO regresiones. Una cosa por vez. Evidencia ANTES de tocar; no asumas causas. Si errás, decilo.**
- Claude Code puede cargar secrets/credenciales por CLI directamente (corren en la máquina de David, los valores no pasan por chat).
- **Reads y diagnóstico:** automatizalos siempre.
- **Writes ADITIVOS / reversibles a la base** (ej: ADD COLUMN IF NOT EXISTS, CREATE POLICY, CREATE TABLE, INSERT … ON CONFLICT DO NOTHING, grants que agregan permisos, crear bucket de Storage, deploy de Edge Functions): corrélos vos por la Supabase CLI y mostrame evidencia de que aplicó. No me los pidas para correrlos yo.
- **Writes DESTRUCTIVOS / irreversibles** (DROP, DELETE, TRUNCATE, ALTER que borra o renombra columnas, migraciones con pérdida de datos, revocar o quitar permisos, rotar o borrar credenciales): NO ejecutes. Backup primero si aplica, mostrame el bloque y el plan, los corro yo.
- **Ante la duda:** si no tenés certeza de que un write es aditivo/reversible, tratalo como destructivo y pedime OK.

## Bitácora viva (regla fija)
- Después de CADA cambio (código, deploy, migración, RPC, secrets), actualizá este archivo en la misma sesión.
- Si el cambio lo hizo alguien por fuera del código (David o Manuel en un dashboard) y me lo informan, registralo igual con fecha.
- Meta: que cualquiera que abra este archivo pueda retomar exactamente desde donde se dejó, sin re-diagnosticar.
- Mantené el archivo CORTO: estado actual, no historial largo. Lo cerrado se resume o se borra.

## Ahorro de tokens (aplicá por defecto, sin que lo pida)
- Mandá la exploración/búsqueda en el código a **subagentes**; que vuelva solo el resumen, no todo el rastreo. Es el mayor ahorro.
- **Avisame** cuando convenga `/clear` (cambié de tema) o `/compact` (sesión larga). **No los corras vos** — son destructivos y dependen de mi criterio.
- Las reglas y el estado viven acá, no en cada prompt.

## Entorno
- React+Vite → Vercel (auto-deploy en push a `main`). Repo: `c:\Users\Dell\Desktop\puntoclases`.
- Supabase ref: `ihwtdblkrxgzhdnzhzsh`. Pagos: Mercado Pago.
- Tooling listo: Node v24, Supabase CLI 2.106 (`npx supabase`, logueada+linkeada), Vercel CLI.
- **Webhook de pagos: unificado en Vercel ✅ (P0 cerrado 2026-07-29).** `api/mp-webhook.js` es el que procesa pagos reales — panel de MP ("Tus integraciones → Webhooks → Modo productivo") apunta a `https://puntoclases.vercel.app/api/mp-webhook`, confirmado con el simulador real de MP (firma generada por MP → 200 OK). `supabase/functions/mp-webhook` (Edge Function, Deno) queda **intacta como rollback** — no se tocó ni se desactivó, no reescribir ni borrar sin decisión explícita.
- ✅ **`DATABASE_URL` en modo transacción (puerto 6543), migrado 2026-07-29.** Antes estaba en modo sesión (5432, `pool_size:15` real de Supavisor — confirmado con el error `EMAXCONNSESSION`), techo real de solo ~15 conexiones concurrentes para cualquier cosa que use `api/_db.js` (los 4 endpoints Vercel: pack/reserva/webhook/reconciliar-huerfanos). Auditoría de compatibilidad previa (prepared statements con nombre, SET de sesión, LISTEN/NOTIFY, transacciones multi-statement, cursores/temp tables, advisory locks de sesión) sin hallazgos — ninguno de los 4 endpoints ni las 5 RPCs que llaman usa algo incompatible con modo transacción. Cambiado en `.env.local` y en Vercel Production (var recreada como `--sensitive` vía CLI). Re-test post-cambio: techo subió de ~15 a **~200 conexiones concurrentes**, conexiones reales a Postgres estables entre 23-32 en todos los niveles probados (multiplexado, no 1:1). El comentario en `api/_db.js` que decía "modo transacción — puerto 6543" ahora es cierto por primera vez.
- MP test: vendedor de prueba `3462408456`. Comprador de prueba `3462408458`. Tarjeta: `4509 9535 6623 3704`, titular **APRO APRO**, 11/30, CVV 123, DNI 12345678.
- **NO pongas secretos en este archivo (se commitea a git).**
- **DB prod**: connection string en `DATABASE_URL` (`.env.local`, ignorado por git). `MP_WEBHOOK_SECRET` también en `.env.local` (cargado 2026-07-25) y en Vercel Production. Para consultar: `psql $DATABASE_URL -c "SELECT ..."` (o vía `pg` desde Node si `psql` no está en el PATH).

## Esquema (ojo)
- La tabla `compras` **NO tiene columna `created_at`** (sí tiene `creado_en`). Leé las columnas reales antes de consultarla.
- `service_role` tiene: `SELECT` en `config` y `packs`; `EXECUTE` en `acreditar_compra`, `registrar_compra_pendiente`, `aprobar_compra`. Ningún grant directo en `alumnos`, `compras`, `reservas`, `profiles`, `mensajes`.
- `acreditar_compra`, `registrar_compra_pendiente`, `aprobar_compra` son SECURITY DEFINER (owner postgres): operan sobre `compras` y `alumnos` sin grants directos.
- `registrar_compra_pendiente(alumno_id, horas, precio, pack_id)` → inserta fila con `estado_pago='pendiente'`, devuelve `id BIGINT`.
- `aprobar_compra(compra_id, payment_id)` → idempotente: si ya está `'aprobado'` devuelve saldo sin tocar nada; si no, actualiza `compras` y `alumnos` en un solo tx.

## ESTADO ACTUAL — al 2026-07-29

✅ **P0 cerrado**: webhook unificado en Vercel, confirmado con MP real (ver "Entorno" arriba). Edge Function intacta como rollback, no tocar.

📋 **Auditoría 2026-08-10** (`docs/auditoria-2026-08.md`, solo lectura, nada tocado): webhook re-confirmado con evidencia fresca — código soporta ambos formatos de notificación de MP desde `dd42dd8`, responde igual (401 firma inválida) a ambos hoy; **sin tráfico real de pagos desde el 22-jul** (última compra aprobada es del 3-jul, 0 reservas con `payment_id`, 0 filas en `pagos_huerfanos`) — el único test real fue el simulador MP del 29/30-jul, falta confirmar con el primer pago orgánico. Bug de "Horas sueltas" en Comprar: **ya estaba arreglado desde el `2d55c46` (16-jun)**, el diagnóstico viejo de este archivo quedó desactualizado. ✅ **RPCs de pago cerradas a PUBLIC/anon (2026-08-10, migración `20260810000002_revoke_public_rpcs_pago.sql`, aplicada)** — era el hallazgo crítico de arriba, confirmado explotable **sin login** con evidencia real en `ROLLBACK` (ver `docs/auditoria-2026-08.md` §7-forense): `acreditar_compra`/`aprobar_compra`/`registrar_compra_pendiente` tenían `EXECUTE` en `PUBLIC` desde que se crearon (~16-jun para `acreditar_compra`; las otras dos ni siquiera tenían migración en git — se versionaron recién ahora). Probado en vivo antes del fix: un alumno cualquiera se acreditó 999hs falsas a sí mismo con `acreditar_compra` y un `payment_id` inventado; y **sin ninguna sesión** (`anon`, sin login) se le acreditaron horas a la cuenta de otro alumno encadenando `registrar_compra_pendiente`+`aprobar_compra` y también con `acreditar_compra` directo (el chequeo `p_alumno_id != auth.uid()` no frena cuando `auth.uid()` es `NULL`). Fix: `REVOKE EXECUTE ... FROM PUBLIC, anon` en las 3. Ojo — esto **también dejó a `authenticated` sin acceso de rebote** (nunca tuvo grant propio, solo heredaba de `PUBLIC`; confirmado con `has_function_privilege` antes/después). Quedan solo `service_role`/`postgres`, que es como corren los endpoints reales (`api/mp-webhook.js`, `api/crear-preferencia-pack.js` vía `pg`, y la Edge Function vieja vía `SUPABASE_SERVICE_ROLE_KEY`) — grep de todo el repo confirmó **cero call sites desde `src/` (cliente)** para `aprobar_compra`/`registrar_compra_pendiente`; están huérfanas de cualquier rol que no sea `service_role`/`postgres`, así que no hay nada más que sacarles — **no DROPear**, siguen siendo las que usa el webhook y crear-preferencia-pack en prod. Forense de `compras` completa (4 filas): las 2 `aprobado` tienen `payment_id` numérico puro de 12 dígitos, formato típico de MP, misma cuenta (`puntoclases@gmail.com`) y fechas ya documentadas (16-jun, 3-jul) — **sin indicios de que el agujero se haya explotado contra datos reales**. Caveat: no se pudo cruzar contra los emails de MP ni el panel (sin acceso a Gmail/MP desde acá); y este chequeo cubre solo el camino vía `compras` — no hay tabla de auditoría de `alumnos.saldo`, así que un cambio de saldo por otra vía no dejaría rastro para revisar después.

✅ **RLS de profe corregida y en prod (2026-08-10, migración `20260810000001_fix_rls_profe_leaks.sql`, aplicada vía `pg`/`DATABASE_URL` porque `supabase migration list`/CLI da 403 en este entorno)** — 2 hallazgos de la auditoría de arriba (§3), verificados con `UPDATE`/`SELECT` reales en transacciones con `ROLLBACK` antes y después de aplicar:
- **Fuga de datos**: `profiles_profe_publico` (cualquier authenticated leía mail/perfil de CUALQUIER profe sin relación) → reemplazada por `profiles_profe_para_alumno` (mismo criterio ya usado en la dirección opuesta, `profiles_alumno_para_profe`: solo si hay una reserva real entre ambos). `profes_select` acotada de "cualquier authenticated" a "vos mismo o admin". `profiles_admin` (ya existía) sigue cubriendo a `getProfesAdmin()` del panel real sin cambios. `profes_publicos` (la vista que usa el buscador de profes del alumno) no se tocó — sigue andando igual, confirmado.
- **Integridad**: `reservas_profe_update` ahora tiene `WITH CHECK` explícito + `GRANT UPDATE` acotado por columna (`estado, devolucion, avance, marcada_en, fecha, hora` — las únicas que el front realmente escribe) en vez de tabla completa. Un profe ya no puede pisar `monto`/`alumno_id`/`costo_saldo`/etc. de sus propias reservas por API directa.
- Verificado post-apply contra prod real (no simulado): profe puede seguir marcando `estado`/`devolucion` (✅ sigue andando), no puede pisar `monto` (🔒 `permission denied`), no puede leer mail de otro profe sin reserva compartida (🔒 0 filas), el buscador de profes del alumno sigue funcionando, y admin real sigue leyendo mail de cualquier profe.
- 🔴 **Bug nuevo encontrado al verificar (no tocado, fuera de este alcance)**: el "reprogramar" del lado ALUMNO (`ModalReprogramar` en `Historial`, línea ~6418) llama `reprogramarReserva()` con un `UPDATE` directo a `reservas` — pero **nunca hubo policy RLS que le diera UPDATE a un alumno sobre sus propias reservas** (solo existían policies para admin y profe). Esto significa que reprogramar ya estaba roto ANTES de esta migración: el `UPDATE` falla por RLS, el error se traga en un `catch(err=>console.error(...))` sin propagarse, y la UI igual muestra "reprogramación confirmada ✓" por estado local optimista — la clase nunca se mueve en la DB pero el alumno ve éxito. Esta migración no lo empeora (ya no funcionaba), pero hay que arreglarlo aparte: falta una policy `reservas_alumno_update` (columnas `fecha`,`hora`,`estado` únicamente) + que el catch de la UI deje de tragarse el error.

✅ **RLS de `alumnos` corregida y en prod (2026-08-10, migración `20260810000003_fix_rls_alumnos_saldo.sql`, aplicada)** — el hueco gemelo del de `reservas`/RPCs de arriba, mismo patrón (`with_check` ausente + `GRANT` de tabla completa). Confirmado explotable en `ROLLBACK` antes del fix: un alumno real, `UPDATE alumnos SET saldo=9999.9 WHERE id=auth.uid()` → 1 fila, pasaba — podía auto-acreditarse saldo (y también `suspendido`/`activo`/`vencimiento`) sin pasar por ninguna RPC.
- Fix: `saldo`/`saldo_residual` sacadas del `GRANT UPDATE` de `authenticated` por completo (solo las tocan RPCs `SECURITY DEFINER` — `acreditar_compra`, `aprobar_compra`, `add_horas_admin`, `devolver_horas` — que corren como `postgres`, no dependen de este grant). `tel`/`suspendido`/`vencimiento`/`activo` quedan otorgadas (las necesita alumno-self + admin), pero `alumnos_self_update` ahora tiene `WITH CHECK` explícito que exige que `suspendido`/`activo`/`vencimiento` no cambien cuando el que escribe es un alumno — comparado contra una función nueva `mis_campos_protegidos_alumno()` (`SECURITY DEFINER`, sin parámetro, hardcodea `auth.uid()` adentro para no abrir un canal de lectura de otro alumno) que sigue el mismo patrón que `mi_rol()` para evitar la recursión de RLS. `alumnos_admin_update` sin cambios. Se dropeó `alumnos_upd` (legacy, duplicada) — confirmado antes que 0 filas reales de `alumnos` tienen `profiles.rol != 'alumno'` o les falta `profiles`, así que no cubría ningún caso que las otras dos policies no cubrieran ya.
- Verificado post-apply contra prod real: alumno intenta `saldo`/`saldo_residual` → `permission denied` 🔒; intenta `suspendido`/`vencimiento`/`activo` → RLS lo rechaza 🔒 (incluso mezclado con un cambio de `tel` legítimo en el mismo `UPDATE`, se rechaza entero); alumno cambia su propio `tel` → ✅ sigue andando; admin cambia `suspendido` de otro alumno → ✅ sigue andando igual que siempre.

✅ **`INSERT` de `reservas`/`compras` cerrado + `resenias` validada (2026-08-10, migración `20260810000004_close_inserts_reservas_compras_resenias.sql`, aplicada)** — barrido completo de RLS post-fixes de arriba encontró 2 huecos más de la misma familia:
- `reservas_insert` no validaba `estado`/`costo_saldo`/`monto`: confirmado en `ROLLBACK` que un alumno podía `INSERT` directo una reserva ya `estado='confirmada', costo_saldo=0, monto=0` — clase gratis, **visible para el profe en su agenda** (probado simulando la sesión del profe, la ve igual que una reserva real), sin pasar por `crear_reserva` ni `crear_reserva_pendiente_pago` (sin validar saldo, disponibilidad, cupo ni solapamiento).
- `compras_ins` mismo patrón: `INSERT` directo con `estado_pago='aprobado'` no acredita saldo (no hay trigger), pero genera un "recibo" falso indistinguible de una compra real en el propio historial del alumno (`Perfil > Compras`).
- `resenias_ins` (relacionado, no financiero): solo exigía `alumno_id=auth.uid()` — un alumno podía calificar a cualquier profe con `reserva_id`/`profe_id` inventados, sin relación real ni que la clase se haya dado.
- Fix: `REVOKE INSERT ON reservas, compras FROM authenticated` (grep completo de `src/` confirmó **cero** `.insert()` directo contra esas dos tablas en todo el cliente — toda la creación real pasa por RPCs `SECURITY DEFINER` que corren como `postgres`, no dependen de este grant). `resenias_ins` reescrita con `WITH CHECK` que exige `EXISTS` una reserva propia, con ese `profe_id`, `estado='realizada'` (confirmado en código — `PuntoClasesApp.jsx:916` — que `'realizada'` es el valor real que usa el front para "la clase ya pasó", no una suposición).
- Verificado post-apply contra prod real: `crear_reserva()` vía RPC sigue funcionando de punta a punta (crea la reserva, descuenta saldo); `INSERT` directo a `reservas`/`compras` → `permission denied` 🔒; reseña de una clase propia `realizada` → ✅ sigue andando; reseña con `profe_id` ajeno a la reserva o de una clase todavía no `realizada` → 🔒 rechazada por RLS.

Con esto se cierran todos los huecos de RLS/grants encontrados en la auditoría del 2026-08-10 y su barrido posterior (`docs/auditoria-2026-08.md`). Pendiente, fuera de este alcance: el bug de "reprogramar" del alumno (ya estaba roto de antes, señalado arriba) y el hallazgo de `profiles.mail`/`creado_en` auto-editables (cosmético, sin flujo sensible que dependa de eso, no priorizado).

✅ **Buscador simple en wizard Reservar + pantalla Profes (2026-08-10, `7971dd8`, push directo a `main`)**: feature aislada, solo frontend, sin tocar backend/DB. `<input>` que filtra mientras se tipea (sin debounce, arrays chicos en memoria) en 3 lugares de `PuntoClasesApp.jsx`: paso 1 del wizard (materia), paso 4 (profe), y el componente `Profes` fuera del wizard. Insensible a mayúsculas y tildes vía helper compartido `normalizarTexto()` (línea ~109, `normalize("NFD")` + regex que saca los diacríticos + `toLowerCase()`). `npm run build` local OK antes de pushear.

## OVERNIGHT 2026-08-10 (sesión sin supervisión en vivo — detalle completo en `docs/overnight-2026-08-10.md`)

✅ **Fase B — reprogramar del alumno arreglado** (migración `20260810000005_reprogramar_reserva_alumno.sql` aplicada + `aa12fb2`): el bug ya señalado arriba (UPDATE directo fallaba silencioso, catch mudo, UI mentía "reprogramado") está cerrado. RPC `reprogramar_reserva_alumno()` `SECURITY DEFINER` (mismo patrón que `confirmar_reserva_pago`): valida dueño, estado, regla 24hs, disponibilidad real del profe y solapamiento — no confía en el front. Grupales explícitamente no soportadas todavía (decisión de alcance, no bug — reprogramar `grupo_id`/cupo es lógica no especificada, ya marcada como límite conocido). `reprogramarReserva()` del profe (vía RLS) queda intacta, se agregó `reprogramarReservaAlumno()` al lado. 7 casos probados en `ROLLBACK` antes de aplicar, todos ✅ (detalle en el doc overnight).

✅ **Fase D — `profiles.mail`/`creado_en` bloqueados** (migración `20260810000006_lock_profiles_mail_creado_en.sql` aplicada + `51352e4`): mismo patrón que `alumnos_self_update` (función `SECURITY DEFINER` sin parámetro + `WITH CHECK`). `rol` ya estaba bloqueado, ahora también `mail`/`creado_en` salvo que sea admin. Sin cambios de frontend (nadie los editaba). 8 casos probados en `ROLLBACK`, todos ✅.

✅ **Fase E — editor de packs en Finanzas ahora persiste** (migración `20260810000007_grant_packs_update_admin.sql` aplicada + `5030f0c`): causa raíz confirmada — `normCfg()` decía en un comentario "packs vienen de tabla separada" pero asignaba el fallback hardcodeado igual, `AppAdminMain` nunca llamaba `getPacks()`, y `authenticated` no tenía NINGÚN grant de escritura sobre `packs` (ni admin). Fix: `GRANT UPDATE (descuento)` acotado a esa columna, protegido por `packs_admin` (ya existía, sin cambios); `AppAdminMain` ahora carga packs reales; `Finanzas.guardar()` persiste cada pack; `actualizarPack()` invalida el cache de `getPacks()` para que `Comprar()` vea el precio nuevo al toque. 3 casos probados en `ROLLBACK`, todos ✅.

✅ **Fase C — carrito de reservas múltiples, verificado y EN PROD (2026-08-11)**: mergeada a `main` (`d691bee`) después de que los 10 puntos del checklist de cierre pasaran con evidencia real — cuenta de prueba dedicada (`carritotest_*`), operaciones REALES (no `ROLLBACK`) contra prod, limpiada al final y verificada en 0 filas en `reservas`/`compras`/`pagos_huerfanos`/`grupos`/`alumnos`/`profiles`/`auth.users`. Alumno arma varias clases antes de pagar; individuales pagan con saldo automático hasta agotarlo (en el orden agregado); grupales SIEMPRE van a MP; el resto (grupales + individuales sin saldo) va en UNA sola preferencia compartiendo `carrito_id` (`external_reference=cart_<uuid>`). Migración `20260810000008_carrito_reservas.sql` aplicada (`ADD COLUMN reservas.carrito_id` + `crear_reserva_pendiente_pago` extendida con `p_carrito_id` opcional). Webhook: RAMA A0 nueva (reusa `confirmar_reserva_pago` en loop, sin tocarla). Bug propio encontrado y corregido en el camino: `CREATE OR REPLACE FUNCTION` con un parámetro nuevo NO reemplaza la función vieja (crea sobrecarga ambigua) — hizo falta `DROP FUNCTION` explícito antes.
- **Verificado con datos reales**: reprogramar/cancelar sobre reserva con `carrito_id` (igual que sin); pago mixto (individual+saldo confirma una vez, grupal+MP confirma con cupo real, saldo no se toca dos veces); carrito 100% saldo (no genera preferencia MP); TTL vencido a mitad de carrito (no traba al resto, libera su slot); tope de 10 ítems (rechazado antes de tocar la DB); quitar un ítem antes de pagar (sin rastro server-side, nunca se llama al backend hasta "Pagar todo"); huérfano parcial (individual confirma, grupal cae a `pagos_huerfanos` con clave sintética, sin duplicar en reintento); regresión de compra de packs y de la reserva de 1 sola clase, ambas intactas. UI del paso 8 mejorada: desglose "cómo se paga cada clase" (saldo vs. MP) visible ANTES de pagar, botón "Quitar" más grande.
- **Límite de evidencia, documentado sin ocultar**: no se pudo hacer el checkout real de MP por navegador ni pegarle al webhook HTTP desplegado — `MP_ACCESS_TOKEN`/`MP_WEBHOOK_SECRET` son variables **solo de Production** en Vercel (una de ellas `--sensitive`, ni siquiera legible por `vercel env pull`), y el Preview de la rama está bloqueado por Vercel Deployment Protection (mismo límite ya documentado en la Fase 3 de escalabilidad). Se sustituyó con la evidencia más fuerte posible: réplica exacta de la lógica de pago mixto contra datos reales committeados (no simulados). Detalle completo de las 10 pruebas en `docs/overnight-2026-08-10.md`.
- **Decisiones de negocio, ya resueltas, no reabiertas**: huérfano parcial de carrito → sin refund automático (igual que 1 sola clase); fallo parcial de pago → sin retry automático, se le pide al alumno recargar; flujo de 1 sola clase → no se unifica a `carrito_id` por ahora; tope de 10 ítems → se mantiene.

✅ **Fase F — limpieza de compras pendientes colgadas** (migración `20260810000009_marcar_compra_fallida.sql` aplicada + `788c9e9`): RPC `marcar_compra_fallida()` transiciona `pendiente→fallido` sin pisar nunca un `aprobado` real; `crearPreferencia()` ahora guarda `compra_id` en localStorage antes de redirigir a MP, se llama al volver con `failure` real (no con `pending`). Solo higiene, no toca saldo. 5 casos probados en `ROLLBACK`, todos ✅. **Con esto se cierra la sesión overnight 2026-08-10** — resumen completo de las 5 fases (B/D/E/F en prod, C en rama para revisión) en `docs/overnight-2026-08-10.md`.

✅ `get_finanzas_periodo()` corregida y en prod (2026-07-29, migración `20260729000001`): tenía un bug real (no de criterio) — `bruto` declarado `numeric` pero `sum(reservas.monto)` daba `bigint`, la función tiraba error en TODA invocación (`structure of query does not match function result type`). Fix: cast `::numeric`. Verificado antes/después simulando sesión admin real contra prod (GUC `request.jwt.claims`, transacción con ROLLBACK).

✅ Finanzas/Dashboard conectados a `get_finanzas_periodo()` server-side (2026-07-29, `cc9a7c5`): `AppAdminMain` llama la RPC una vez, suma los períodos para el total histórico, pasa `finanzas={bruto,pagoProfe,costoCowork,neto,cargando,error}` a `Dashboard`/`Finanzas`. Cálculo viejo queda comentado (no borrado). Error de RPC se muestra prolijo en vez de romper la pantalla. **Paridad verificada solo trivialmente** (0=0, no hay reservas `'realizada'` en prod todavía) — falta confirmar con números reales no-cero.

✅ **Techo de conexiones resuelto (2026-07-29)**: ver "Entorno" arriba — `DATABASE_URL` migrado a modo transacción (6543), techo subió de ~15 a ~200 conexiones concurrentes.

✅ Backend de pagos reescrito a `pg`/`DATABASE_URL` (2026-07-22, `dd42dd8`): `api/crear-preferencia-pack.js`, `api/crear-preferencia-reserva.js`, `api/mp-webhook.js` en Vercel, sin depender de `SUPABASE_SERVICE_ROLE_KEY`. Auth híbrida: funciones con `auth.uid()` interno (`crear_reserva_pendiente_pago`) vía JWT/PostgREST; el resto vía `pg` directo (rol `postgres`, ya tiene EXECUTE en todas las RPC).

✅ Carrito multi-reserva revertido (2026-07-22, `75fb3c7`): vuelta a una clase por vez. La rama `mr_...` sigue existiendo (muerta) en la Edge Function vieja.

✅ Rate limiting en prod (2026-07-24/25): tabla `rate_limits` + función `chequear_rate_limit` (ventana fija, atómica, `EXECUTE` solo `postgres`) aplicadas y **verificadas en DB** (existencia + grants + filas reales de uso). Activo en los 3 endpoints Vercel (pack/reserva/webhook). Migración `20260724000001_rate_limits.sql`.

✅ Sentry instrumentado (2026-07-24, `7c61513`): front (`@sentry/react`) + los 3 endpoints Vercel (`@sentry/node`, `api/_sentry.js`). Scrubbing de secrets/PII. Requiere `SENTRY_DSN`/`VITE_SENTRY_DSN` cargados en Vercel (ya están). **No cubre la Edge Function** (ver P0).

✅ Paginación admin (2026-07-25, `74b268e`): `getAlumnos`/`getProfesAdmin`/`getTodasLasReservas` con `{page, pageSize}` + `.range()`. Los agregados de Dashboard/Personas/Finanzas siguen trayendo el set completo (pageSize alto) para no romper conteos — con `console.warn` si se trunca.

✅ Cache config/packs (2026-07-25, `84b46e2`): TTL 5min en memoria del cliente, `updateConfig` refresca el cache al guardar.

✅ Refund de pago huérfano fuera del camino crítico (2026-07-25, `9242332` + fix `8e7ad35`): el INSERT-first en `pagos_huerfanos` queda `await`-eado en el crítico (si falla, bubblea a 500 real → MP reintenta); solo el refund a MP + update de motivo van en background vía `waitUntil` (`@vercel/functions`). *Ojo: este fix es solo para `api/mp-webhook.js` — la Edge Function nunca tuvo este bug porque nunca tuvo el patrón async que lo causaba.*

✅ Endpoint de reconciliación de huérfanos (2026-07-25, `1e3b014`): `GET /api/admin/reconciliar-huerfanos` (admin-only), solo lista y loguea filas `pendiente`/`refund_pendiente` de `pagos_huerfanos`. **Refund automático deshabilitado a propósito** (código comentado, activar requiere OK explícito de David).

⏸️ `npm audit fix` parcial (2026-07-25, `0d7c383`): resueltos `fast-uri`/`postcss`. Queda `brace-expansion` (alta, transitivo de `vite-plugin-pwa`→`workbox-build`, requiere `--force` y bajar `vite-plugin-pwa` a 1.2.0 — breaking change, no aplicado, riesgo real bajo por ser build-time).

✅ `invalidatePacksCache()` borrada (2026-07-25, `bc41f61`) — código muerto confirmado, no hay ningún escritor de `packs` en el repo.

--- histórico (2026-07-02 a 2026-07-15), cerrado, sin acción pendiente ---

✅ Fix definitivo del bug original de acreditación (2026-07-02): MP Checkout Pro no propaga `metadata` al payment → patrón "compra pendiente en DB" (Opción B), `external_reference` = id numérico, 500 en vez de 200 silencioso.
✅ `MP_WEBHOOK_SECRET` rotado 2026-06-24 (valor viejo había pasado por chat).
✅ `devolver_horas` bigint fix (2026-07-05, `cb395f3`).
✅ F6 Etapa 1 (migraciones `pendiente_pago`/`expirada`, `pagos_huerfanos`, `crear_reserva_pendiente_pago`, `confirmar_reserva_pago`) y Etapa 2a (backend pago por clase + `handleOrphan`) — aplicadas a prod el 2026-07-05, en su momento en rama `f6-review`.

## INVARIANTES DE INTEGRIDAD (obligatorias en todas las fases)
1. Dinero y saldo solo se mueven server-side en RPCs atómicas e idempotentes (clave: payment_id / reserva_id). El front jamás confirma pagos ni descuenta saldo.
2. Toda reserva paga nace `'pendiente_pago'` con TTL de 30 min que retiene cupo; el webhook la confirma vía external_reference; vencida, libera cupo. Revalidar cupo al aprobar.
3. Cupo grupal y overlap de slots se validan y descuentan DENTRO de la transacción SQL (lock), nunca en el front.
4. La tabla `reservas` es la única fuente de verdad del calendario (alumno y profe leen de ahí, sin copias).
5. Precios se leen de la DB también para display (nada de precios hardcodeados en el front).
6. Al volver de MP: polling del estado real en DB (cada ~3s, mensaje "Estamos confirmando tu pago…"), no setTimeout ciego.
7. Cancelaciones: RPC atómica idempotente con regla de 24hs (nunca devolver dos veces).

Toda fase nueva se diseña para que violar estas reglas sea imposible, y agrega verificación de esto en sus criterios de aceptación.

## REDISEÑO — COLA DE FASES
- **F1–F5.1 ✅** (2026-07-04/05): design system accesible, wizard reserva 8 pasos, agenda del alumno rediseñada, grupal real (tabla `grupos`, cupo en vivo, `unirse_grupo`/`get_grupo_info`). Todo en prod, sin cambios desde entonces.
  - **RADAR F6 pendiente**: cancelar grupo → devolver saldo a todos los inscriptos · reconciliar tipo 'ambas' de disponibilidad.
  - **LÍMITE CONOCIDO (no bloqueante)**: `unirse_grupo` no valida solapamiento entre dos grupos del mismo profe a horas distintas.
- **F6 (Etapas 1, 2a, 2b, 2c) ✅ — en `main`, no en `f6-review`**: el pago por clase (reserva → `pendiente_pago` con TTL → pago MP → `confirmar_reserva_pago` → huérfano/refund si corresponde) está completo y mergeado. La bitácora vieja decía "en rama f6-review" para 2b/2c — quedó desactualizada; confirmado en código que `api/crear-preferencia-reserva.js` y el flujo completo viven en `main` desde el push del 2026-07-22.
  - **VERIFICAR pendiente (no bloqueante, no crítico hoy)**: reprogramar una grupal pagada de extremo a extremo — cupo en el nuevo horario, re-agrupación correcta.
- **F7** (opcional, no arrancado): carrito progresivo "Agregar otra clase".

## AUDITORÍA DE ESCALABILIDAD — cola aparte (track distinto al rediseño)
- **FASE 1 ✅**: diagnóstico completo, solo reporte (10 áreas: DB/pooler, índices, RLS, caching, paginación, async, rate limit, monitoreo, realtime). Sin cambios de código.
- **FASE 2 ✅ (cerrada 2026-07-29)**: índices, rate limiting, Sentry, paginación admin, cache, refund async, reconciliación huérfanos, `get_finanzas_periodo` (con su fix), P0 del webhook. Pendiente de decisión (no bloqueante): `npm audit --force` (bajaría `vite-plugin-pwa`, breaking change).
- **FASE 3 ✅ (2026-07-29)**: load testing ejecutado contra Preview (`dpl_GvYD7GvbcX5f4TpVir4C7v2oWtB8`) — con la salvedad de que Vercel Deployment Protection bloquea requests directos a las URLs de Preview, y `DATABASE_URL`/`MP_ACCESS_TOKEN`/`MP_WEBHOOK_SECRET` no están scopeados a Preview en Vercel (solo Production). El test real se corrió contra Supabase directo (PostgREST + `pg`), que es donde vive el cuello de botella real. Reads vía PostgREST (front) escalan bien a 300 concurrentes sin error. Reserva concurrente sobre el mismo slot: sin doble booking, funciona bien. Hallazgo principal — **techo de conexiones de Supavisor (~15 concurrentes en modo sesión)** — ya resuelto (ver "Entorno" arriba, techo subió a ~200). Datos de prueba del load test (cuenta `loadtest_...@puntoclases.test` y su reserva asociada) limpiados de la DB.
- **Objetivo declarado**: soportar ~15 profes × 20 alumnos (hasta 300 usuarios). El bloqueante real encontrado (techo de conexiones de Supavisor) ya está resuelto.

## Regla de negocio — Vencimiento de horas (fija)
- `saldo >= 0.8 hs` cuando vence → se pierde todo (saldo = 0). La clase mínima es 0.8 hs; si tenés menos no podés reservar.
- `saldo < 0.8 hs` cuando vence → se conserva (remanente no reservable se suma a próxima carga).
- La regla se aplica en `acreditar_compra` al recargar (punto atómico, idempotente por ON CONFLICT).
- Front: `saldoVivo(sal, venc)` en `AlumnoApp` → `saldoDisplay` se pasa a todos los componentes que muestran saldo (header badge, Inicio, Perfil, Reservar)..

## A futuro (no prioritario)
- **Google Calendar**: sincronizar reservas al Calendar del alumno y del profe. Requiere Google Cloud project con Calendar API habilitada + OAuth 2.0 credentials de Google. Iniciarlo cuando se decida.
- **WhatsApp**: notificaciones vía WA Business. Requiere cuenta Business en Meta verificada — el trámite tarda, conviene iniciarlo con tiempo.
- **Reembolsos/contracargos — HUECO DE FLUJO DE DINERO**: el webhook ignora pagos con `status != "approved"` (correcto, no acredita). PERO si un pago ya acreditado pasa luego a `refunded` o `charged_back`, MP envía notificación y el webhook vuelve a retornar 200 sin acción: el alumno conserva las horas, `compras.estado_pago` queda en `'aprobado'`. No hay descuento automático de horas ni cambio de estado. Solución pendiente: handler para notificaciones de reembolso que (a) actualice `compras.estado_pago = 'reembolsado'` y (b) descuente `alumnos.saldo`. **No implementar aún** — requiere diseño (¿qué pasa si el alumno ya usó las horas?).
- **Pago al profe — FALTA (no es bug)**: el flujo de dinero alumno→profe no está implementado. Lo existente es solo visual: campo `pagado_mes` (boolean manual) en la vista de Ingresos del profe, y una alerta de cierre mensual sin automatización. No hay cálculo server-side del monto a pagar al profe, ni transferencia real, ni liquidación automática. Pendiente diseñar: modelo de comisión, frecuencia de pago, integración con MP para transferencias o instrucciones de pago manual.
